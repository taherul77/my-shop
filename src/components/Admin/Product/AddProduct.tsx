import React from 'react'
interface AddProductProps {

    modalClose: (open: boolean) => void;
  
  }
const AddProduct = ({modalClose}:AddProductProps) => {
  return (
    <div>
      EditProduct
    </div>
  )
}

export default AddProduct
