import React from 'react'
interface AddBrandProps {
    modalClose: (open: boolean) => void;
  }
const AddBrand = ({modalClose}:AddBrandProps) => {
  return (
    <div>
      
    </div>
  )
}

export default AddBrand
